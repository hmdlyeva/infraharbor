# Roadmap

The implementation roadmap is tracked with stable task IDs (`IH-###`). The canonical planning workbook and detailed feature specifications live in the project Google Drive workspace. This directory mirrors enough task metadata for contributors and future implementation sessions to work from the repository without relying on conversation history.

Task execution order is dependency-driven. Each implementation commit must reference its task ID.
